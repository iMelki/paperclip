## Thinking Path

> - Paperclip manages AI agents and their work.
> - Local startup can use embedded PostgreSQL.
> - Direct postgres startup can fail under an elevated Windows token.
> - Current dev still uses direct startup and taskkill shutdown.
> - This PR routes Windows startup and shutdown through pg_ctl.
> - A local correction handles spawn errors and requests hidden pg_ctl windows.

## Linked Issues or Issue Description

Refs #35. Acceptance remains open.
[Independent review](https://github.com/iMelki/paperclip/pull/117#issuecomment-5648622603).

## Agent / Author

- Original author: Claude Code, recorded in the original commit.
- Correction: OpenAI Codex.
- Human reviewer: repository owner; final approval pending.

## Scope

- Allowed: vendor patch, mocked tests, one existing policy-job test step, gate evidence, tracking notes, PR body, lockfile restoration.
- Out of scope: other runtime changes, governance changes, dependency labels, elevated runs, heavy tests.

## What Changed

- Existing head `7bab6d10ea3ef1a8cdda437bba13d58dcae8b0b2` uses pg_ctl start/stop on Windows and recovers the postmaster PID.
- Local correction adds error listeners and `windowsHide: true` to both new spawns.
- Original `-o` join semantics remain unchanged; tests cover ordinary flags only.
- Nine lightweight mocked tests cover launch failures, exit failures, arguments, PID recovery, and shutdown.
- The existing policy job explicitly invokes the tests with Node 24; root Vitest does not discover scripts.
- Local lockfile matches base `9f4a7e0aa99cd964fe7cd8913981500cc8f13d2f`. CI regenerates it for patch changes.

The parent approved the narrow correction for publication on 2026-09-13, not
merge. Independent rerun: 9 passed, 0 skipped, 35.2 ms, with clean diff check.
Normal hooks remain required. Publication status must be confirmed separately;
the branch's original history remains intact.

Publication checkpoint: normal tracked pre-commit hook exited 1 on 2026-09-13
because this worktree lacks `cli/node_modules/tsx/dist/cli.mjs`, required by the
workspace-link preflight. Token and Gitleaks checks passed. No commit was created;
push was not attempted. No hook bypass or dependency install was used.

Subsequent authorized recovery: one offline, ignore-scripts pnpm 9.15.4 install
attempt failed after 5.03 seconds with ERR_PNPM_NO_OFFLINE_META for
`hono@>=4.11.4 <5.0.0-0`, a dependency of `@modelcontextprotocol/sdk@1.29.0`.
Downloaded 0; added 0. The base lockfile is unchanged. No fallback, lifecycle
build, shared dependency junction, commit retry, or push followed.

## Verification

- Local: `node --test --test-isolation=none scripts/embedded-postgres-pgctl.test.mjs`: 9 passed, 0 failed.
- Local: Node syntax check, patch numstat parser, diff whitespace check, and base lockfile blob comparison passed.
- Negative proof: false windowsHide flags caused six attributed failures and exit 1. Restored flags passed all nine tests with exit 0; the patch hash matched. Evidence is recorded locally in `.gate-evidence.json` and `doc/evidence/pr117/`.
- Original author reported an unelevated database smoke pass. This correction did not repeat it.
- No database, elevated test, install, full build, or broad test suite ran due to host memory pressure.
- Existing CI is not all green. Elevated acceptance remains open; the parent approved only the narrow correction checkpoint.

## Recovery update (2026-09-13)

Earlier missing-dependency checkpoints below are historical. Registry-enabled
provisioning subsequently succeeded (442 cached packages linked, no archives
downloaded), followed by the repository's plugin build-dependency helper.
The normal pre-commit hook created correction commit `36182c123`; token and
Gitleaks checks passed, and related Vitest discovery selected no suites.
The nine focused Node tests passed separately. React Doctor is disabled/incomplete,
not passed. Push and hosted checks are still pending; MERGE HOLD remains.

## Risks

- MERGE HOLD: existing `pg_ctl -o` CMD forwarding requires dedicated native command-line proof. Parent research found PostgreSQL 18 wraps post_opts in `ComSpec /C` at pg_ctl.c lines 517-522. No safe handling of quotes, percent expansion, exclamation marks, or shell metacharacters is claimed. The proposed CRT quote map was removed.

- Elevated initdb/data-directory ACL compatibility remains unverified.
- Mock tests cannot prove native pg_ctl argument parsing or full package integration.
- `windowsHide` on the two pg_ctl launches does not prove all descendant windows are absent.
- Trusted dependency review and the user's all-green condition remain required. No dependency label was added.
- The parent reports branch-protection API 404; this does not waive the review condition.

Research verified by the parent:
[Node spawn errors](https://nodejs.org/api/child_process.html#event-error),
[pg_ctl documentation](https://www.postgresql.org/docs/current/app-pg-ctl.html),
[upstream shutdown issue 38](https://github.com/leinelissen/embedded-postgres/issues/38),
[PostgreSQL 18 pg_ctl source](https://raw.githubusercontent.com/postgres/postgres/REL_18_STABLE/src/bin/pg_ctl/pg_ctl.c).

## Model Used

- Original commit records Claude Sonnet 5 via Claude Code. Exact API model ID and context window were not recorded.
- Correction: OpenAI GPT-6 in Codex, with code editing and tool use. Exact runtime model ID and context window are not exposed to this task.

## Checklist

- [x] I have included a thinking path that traces from project context to this change
- [x] I have specified the model used (with available version and capability details)
- [x] I have checked ROADMAP.md and confirmed this is a bounded bug fix
- [ ] I have searched GitHub for duplicate or related PRs and linked them above
- [x] I have linked an existing public issue with Refs #35
- [x] I have not referenced internal or instance-local Paperclip issues
- [x] My branch name describes the change and contains no internal ticket id
- [x] I have run the documented lightweight local tests and they pass
- [x] I have added tests in the local correction
- [x] I have updated local tracking documentation
- [x] I have considered and documented risks
- [ ] All Paperclip CI gates are green
- [x] Greptile review: N/A — not configured; no score or completion claimed
- [ ] If Greptile is configured, I have linked its review and resolved findings
- [x] Related issue is linked
- [x] Scope matches the issue and unrelated work is excluded
- [ ] Human review of the amended head is complete
- [ ] All applicable reviewer comments are resolved
- [ ] Elevated Windows startup/shutdown acceptance is verified
- [ ] The correction is committed and pushed
